# Python
__pycache__/
*.py[cod]

# editors / OS
.vscode/
.idea/
Thumbs.db
desktop.ini

# local scratch
*.log

# build output (python tools/make_release.py)
dist/

# generated at install / editor-save time, never committed
coat_side/actions/menus/

# agent config files stay on this machine - they are never part of a published
# repository (2026-09-23). The files exist locally, they are just not ours
# to hand out: they hold working rules, absolute paths and names of local folders.
AGENTS.md
CLAUDE.md
.cursorrules
.cursor/
.github/copilot-instructions.md

# session handoffs are internal too: they are written for the next agent session
# on this machine, with local state and paths in them. Same rule as AGENTS.md.
docs/handoffs/

